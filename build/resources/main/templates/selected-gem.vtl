
<h1>Select another gem for $gem.getGemName() to fuse with:</h1>

#foreach($potential_pair in $gem.getPotentialPairs())
  <form action='/fusion' method='post'>
  <input id="gem_id" name="gem_id" type="hidden" value="$gem.getId()">
  <input id="pair_id" name="pair_id" type="hidden" value="$gem.getPotentialPairs()">
  <img src="/src/$gem.getLink().gif"></a>
#end
